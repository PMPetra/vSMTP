obj addr "john" "johndoe@personnal.com";

rule rcpt "test_add_rcpt" #{
  condition: || vsl.IS_RCPT("john"),

  // I want to track every mail sent to john.
  on_success: || {
    vsl.ADD_RCPT("me@personnal.com");
    vsl.CONTINUE()
  },

  on_failure: vsl.CONTINUE,
};