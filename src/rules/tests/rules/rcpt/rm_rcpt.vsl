obj fqdn "satan" "satan.org";

rule preq "test_rm_rcpt" #{
  condition: || true,

  on_success: || {
    for r in rcpts {
      rcpt = r;
      if vsl.IS_RCPT("satan") {
        // quietly removing the recipient.
        vsl.DEL_RCPT(rcpt);
      }
    }
    vsl.CONTINUE()
  },
};